
<div>
	{{burger_name}}

	<button class="change-devour" data-id="{{id}}" data-newdevour="{{devoured}}">
		{{#if devoured}}Devour It!{{else}}compost it!{{/if}}
	</button>

	<button type="button" class="delete-burger" data-id="{{this.id}}">Compost IT!</button>
</div>