
<div class="burger-div">
	<div class="row">
		<div class="col-8">
			{{burger_name}}
		</div>
		<div class="col-4">
			<button class="change-devour btn btn-outline-secondary rounded-0" data-id="{{id}}" data-newdevour="{{devoured}}">
				{{#if devoured}}Devour It!{{else}}Compost it!{{/if}}
			</button>
		</div>
	</div>
</div>